import { createZodDto } from 'nestjs-zod'; import { z } from 'nestjs-zod/z'; export const AiMessageSchema = z.object({ role: z.enum(['user', 'system', 'assistant', 'tool', 'data']), content: z.string() }).passthrough(); export const ChatRequestSchema = z.object({ messages: z.array(AiMessageSchema), options: z.any().optional() }).passthrough(); export class ChatRequestDto extends createZodDto(ChatRequestSchema) {}
